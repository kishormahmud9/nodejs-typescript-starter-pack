model OTPVerification {
  id         String   @id @default(uuid())
  email      String
  otp        String
  purpose    String?   // forgot_password
  expiresAt  DateTime
  verified   Boolean  @default(false)
  createdAt  DateTime @default(now())
}